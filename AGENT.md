AGENT.md - 幸福设计系统 AI 开发指令
1. 项目概述
幸福设计系统是一款个人状态管理应用。核心业务流为：通过 AI 引导用户进行语音或文本对话，按既定顺序探查用户在意义、意志、经济、健康、人际、美德、热爱、擅长这8个维度的日常行为。系统依据客观事实为各项要素进行打分（1至10分），并最终生成包含量表、数据趋势和干预建议的复盘日记。
你的任务是作为核心开发助手，按照既定的技术架构编写、重构和优化系统代码。
2. 技术栈与开发规范
后端开发 (Python + FastAPI)
API 设计：采用 RESTful 风格定义常规接口。对话接口需使用 Server-Sent Events (SSE) 或 WebSocket 实现流式输出。
数据存储：使用 PostgreSQL 配合 SQLAlchemy（或同类 ORM）处理持久化数据。使用 Redis 处理多轮对话的状态机与上下文缓存。
业务解耦：严禁将大语言模型的调用逻辑与数据存取逻辑耦合。需划分清晰的路由层（Routers）、服务层（Services）和数据层（Models）。
前端开发 (跨端框架 uni-app / Taro)
终端目标：代码需具备良好的跨端兼容性，首期保障 H5 网页端的平滑体验，架构层面预留直接编译为微信小程序的通道。
图表渲染：统一使用 ECharts。需做好组件封装，确保在 H5 和小程序环境下的挂载逻辑正常。
流式渲染：对话界面需妥善处理后端下发的字节流，实现稳定的打字机效果。
3. 代码风格与规范
命名约定： 变量与函数命名需准确反映业务含义（例如使用 calculate_element_score 替代 get_score）。
类型声明： Python 代码需提供完整的 Type Hints。前端尽量采用 TypeScript 进行静态类型约束。
注释要求： 避免废话注释。仅在复杂的数据转换逻辑、大模型提示词拼接规则以及状态机跳转处添加清晰的逻辑说明。
错误处理： 统一全局的错误拦截机制。向前端返回的错误结构需保持一致，包含明确的业务状态码。
4. 测试要求
核心逻辑覆盖： 针对8要素的打分逻辑、历史数据趋势的计算逻辑编写单元测试。
大模型边界测试： LLM 的输出具备不确定性。需针对 AI 返回异常格式、缺失字段或拒绝回答的场景编写兜底逻辑，并提供对应的测试用例。
接口契约测试： 前后端联调前，需提供完整的 API 交互示例数据。
5. 关键业务注意事项
JSON 解析容错： 在要求 AI 输出最终的日记结构化数据时，后端解析模块需具备强健的容错和重试机制，防止因 JSON 格式损坏导致程序崩溃。
对话流程控制： 大模型在多轮对话中容易发散。后端的会话控制服务需精准记录当前探查的要素节点，并在每次请求的上下文中动态注入当前状态，强制 AI 按顺序推进。
数据隔离： 严格通过 user_id 划分数据权限，任何查询操作默认带上用户身份标识。
